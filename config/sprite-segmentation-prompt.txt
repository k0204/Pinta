输出协议（最高优先级）：
- 此响应将由程序直接作为 JSON 解析。
- 不要回复确认、计划、进度、分析过程或结论说明，也不要复述任务。
- 完成图像分析后只发送一次最终响应，内容只能是下文指定的 JSON 对象。
- 响应的第一个字符必须是 {，最后一个字符必须是 }。

分析输入的角色精灵图集，检测其中的每一个角色方向图，并为程序提供独立裁切矩形和脚底对齐坐标。程序会严格按照你返回的 bbox 裁切原图，再按照 foot_anchor 将每个裁切结果放入同一个输出画布；你不需要输出裁切后的图片、网格坐标、统一尺寸或放置坐标。

这些图片属于同一个角色、同一个静止动作，但朝向不同。目标是将它们拆分成独立图片，并在游戏中切换方向时保持角色脚底位置稳定，不发生水平或垂直跳动。

跨方向注册是本任务的核心目标：把所有方向视为同一个角色站在同一个世界位置的不同观察结果。bbox 可以因朝向造成的自然轮廓变化而不同，但裁剪后所有方向的 foot_anchor 必须代表同一个角色根部注册点。程序会把每个方向的这个注册点移动到同一个输出目标点；如果只是角色的可见轮廓变宽、变窄、左右不对称或出现不同留白，不得因此改变注册点。

坐标与检测规则：
1. 必须按当前上传 PNG 的画布坐标分析，不得自行旋转或重新采样图像。程序可能在上传前按比例缩小过大的 PNG；提示词末尾给出的宽高始终是当前上传图的实际像素尺寸，必须原样返回这两个值，不得根据可见内容、模型预览尺寸或内部缩放结果猜测 image_width 和 image_height。程序会在收到结果后将坐标还原到原始图像。
2. 使用像素坐标系，原点为整张图像左上角，x 向右增大，y 向下增大。
3. 检测图集中所有不同的角色图片，不得遗漏、重复或合并相邻角色。
4. 按视觉行分组：先从上到下排列各行，再在同一行内从左到右排列。
5. index 按上述阅读顺序从 0 开始连续编号。
6. 同一角色图片中的头发、武器、衣物、饰品、轮廓、抗锯齿边缘、与身体分离的小块像素，以及属于该帧的阴影或特效，都必须归入同一个 item。
7. 背景、网格线、标注文字和不属于任何角色帧的杂点不得计入 bbox。
8. 不得仅按连通区域拆分角色；同一帧中彼此不连通但明显属于该角色的像素仍属于同一个 item。

分析步骤（必须按此顺序执行）：
1. 先确定整张上传图的实际宽高，再按视觉行从上到下、从左到右找出每个独立角色帧；不要假设存在规则网格，也不要用固定列宽、固定行高或平均单元尺寸切分。
2. 对每个角色帧单独收集全部属于它的像素，包括透明间隔中的头发、武器、衣摆、饰品、阴影、特效和脱离主体的小像素；相邻角色必须保持在不同 item 中。
3. 先横向比较同一角色的所有方向，确定共同的站立基准、脚底基线和角色根部注册标准；再对每个角色帧独立计算 bbox 和该基准在源图中的绝对坐标。一个角色帧可以比另一个角色帧更宽、更窄、更高或更矮；bbox 不需要互相对齐，也不需要相同尺寸。
4. 返回前逐项复核：bbox 能覆盖该帧的所有突出部位，foot_anchor 的 x 是真实人体中轴投影、y 是真实脚底接地基线，并符合所有方向的共同注册标准；foot_anchor 仍须落在对应 bbox 内或其边界上。只有完成复核后才生成 JSON。

bbox 规则：
1. bbox 必须完整包含该帧的全部有效像素，并在角色轮廓四周保留透明安全边距；宁可多保留透明区域，也绝不能贴着角色轮廓裁切。
2. 必须分别检查头顶、左右两侧和脚底四条边界。尤其要独立检查两条腿、两只脚、头发末端、武器末端、衣摆、饰品和分离像素，不得只根据身体主体或 foot_anchor 推断 bbox。
3. 如果角色靠近画布边缘，安全边距可在该侧缩小到画布边界；如果相邻角色限制了空间，应以两者之间透明间隔的中线为界，但仍不得包含相邻角色像素。
4. 不得为了让不同方向尺寸一致而统一 bbox。每个方向必须根据自己的完整轮廓和安全边距独立计算。
5. 先找出该帧所有有效像素的 xmin、ymin、xmax、ymax，再向四周加入安全边距并限制在当前上传画布内。bbox.x、bbox.y 使用加入安全边距后的左上坐标。
6. 加入安全边距后的最右边界记为 right，最下边界记为 bottom，则 width = right - x，height = bottom - y；right 和 bottom 是不包含端点的像素边界坐标。
7. bbox 的 x、y、width、height 必须为整数，width 和 height 必须大于 0。
8. bbox 是程序唯一使用的裁切区域，不是建议区域。过小会裁掉像素，过大会吞入相邻角色；因此必须优先保证完整性，再在不包含相邻角色的前提下保留最小安全透明边距。
9. 禁止把白色背景、透明背景、网格线、列线、行线、文字、标记或相邻角色的像素放进 bbox。若背景颜色与角色接近，按角色轮廓、边缘连续性和相邻帧归属判断，不要把整块背景当作角色。

脚底锚点与对齐规则：
1. foot_anchor 表示角色站立在地面上的稳定注册点，不是每一帧独立的视觉中心。foot_anchor.x 必须是骨盆和躯干形成的真实人体垂直中轴投影到脚底基线上的 x 坐标；foot_anchor.y 才依据脚掌与地面的实际接触位置确定。必须先比较所有方向，再使用同一种人体中轴和脚底基线标准。
2. foot_anchor 使用整张当前上传图集中的绝对坐标，不是 bbox 内的局部坐标。
3. 不得直接使用 bbox 中心、角色轮廓中心或 bbox 最下方作为脚底锚点。
4. 即使 bbox 包含低于脚底的武器、衣摆、尾巴、阴影或特效，foot_anchor.y 仍必须对应真实脚底接地基线；阴影和特效不得参与锚点计算。foot_anchor.x 不要求落在脚部像素上，可以落在两脚之间，因为它表示人体中轴在地面上的投影。
5. 确定 foot_anchor.x 时，先寻找骨盆中心、两侧髋部连线的中点或躯干根部，再沿角色的站立方向投影到脚底基线：
   - 正面或斜 front 方向：以两侧髋部/骨盆中心和躯干中心推断人体中轴，不以双脚中心代替。
   - 侧面方向：以骨盆和躯干的中心线推断人体中轴，不以可见脚、脚尖或脚跟代替。
   - 如果骨盆部分被服装、武器或遮挡物覆盖，根据躯干中心、腿根连接处和整体站姿推断中轴；可见脚只用于确定 foot_anchor.y，不用于定义横向中心。
6. foot_anchor.y 取脚底与地面接触处的水平基线。采用像素边界坐标，因此它通常等于接地鞋底最下方像素行的 y + 1。
7. foot_anchor.x 和 foot_anchor.y 可以是整数或小数；需要表达两个像素边界之间的精确中心时可以使用 .5。
8. 对所有方向必须使用同一种解剖学和几何学标准。不同方向的可见脚、腿和身体轮廓会自然变化，但不能为每个方向重新选择不同的脚中心、脚尖、最低点或视觉中心；人体中轴注册标准必须保持一致。
9. 如果各方向的脚底像素因绘制误差出现轻微高低差，使用所有方向共同的地面基线和角色根部位置判断，选择最能代表同一站姿的承重点；不要把单个方向的离群像素、阴影、特效、衣摆或武器末端当作基线。
10. 如果某个方向只露出一只脚，仍根据骨盆、躯干和腿根推断同一人体中轴；不得把可见脚的中心、脚尖、脚跟或外侧边缘当作角色中心。不得因为可见脚在画面中偏左或偏右就改变同一角色的整体注册位置。只有确实发生不同站姿或动作时，才允许注册点改变，本任务中不得假设存在这种变化。
11. 共同注册标准不意味着不同帧必须拥有相同的源图绝对 x/y；帧可以位于源 PNG 的不同位置。关键是每帧的局部 foot_anchor 被移动到同一个输出目标点后，角色的承重根部和脚底基线保持稳定。
12. 裁剪后的局部锚点由以下公式得到，但不要额外输出 local_anchor：
   local_anchor_x = foot_anchor.x - bbox.x
   local_anchor_y = foot_anchor.y - bbox.y
13. 程序会把每个裁切图的局部锚点移动到同一个输出目标点（输出画布水平中心、底部基线）。因此你不能用 bbox 的左上角、中心、底边或不同方向的视觉中心代替 foot_anchor；只要脚底坐标正确，即使每个 bbox 尺寸完全不同也能正确还原位置。

禁止的推断和输出：
- 不要假设 4x2、4xN 或任何固定网格；不要为了适配网格而扩大、缩小、平移或合并 bbox。
- 不要把同一角色的不同方向强行裁成相同宽高；不要为了尺寸一致而裁掉透明安全边距。
- 不要使用 bbox 中心、bbox 底边、角色头部中心、脚中心、脚尖、脚跟或阴影最低点作为 foot_anchor；foot_anchor.x 必须来自骨盆/躯干的人体中轴投影。
- 不要为每个方向分别选择不同的脚底基线；不要让单个方向的最低可见像素决定 foot_anchor.y。
- 不要因为某个方向只有一只脚可见，或两只脚的可见位置不同，就把可见脚位置当作人体中轴或改变同一角色的整体根部注册标准。
- 不要输出 local_anchor、cell、row、column、x_position、y_position 或任何未声明字段。
- 不要因为某个角色帧看起来偏左、偏右、偏高或偏低而修改其真实 bbox；位置还原由程序根据 foot_anchor 完成。

返回前必须自行检查：
1. image_width 和 image_height 必须与提示词末尾给出的已知实际画布尺寸完全一致。
2. 所有角色帧均已检测，且 index 顺序正确并从 0 连续递增。
3. 每个 bbox 都完整包含对应帧的全部有效像素，并已逐边确认安全边距；任何一只脚或其他突出部位都没有越过 bbox。
4. 每个 foot_anchor.y 都对应真实脚底接地基线，foot_anchor.x 都是骨盆/躯干人体中轴在该基线上的投影，而不是脚尖、脚跟、脚中心、轮廓或 bbox 的几何中心；x 可以位于两脚之间。
5. 每个 foot_anchor 都严格包含 x 和 y 两个字段。
6. 不同 item 的 bbox 可以有不同的 x、y、width、height；不要为了看起来整齐而修改它们。
7. 每个 foot_anchor 都使用整张上传图的绝对坐标；程序会自行计算局部锚点并完成最终放置。
8. 从所有方向分别减去各自 bbox 左上角后，得到的局部 foot_anchor 都符合共同的站立基准；模拟把每个局部锚点移动到同一输出目标点时，角色不会因换向产生水平或垂直跳动。

严格返回以下结构：{"image_width":整数,"image_height":整数,"items":[{"index":整数,"bbox":{"x":整数,"y":整数,"width":整数,"height":整数},"foot_anchor":{"x":数字,"y":数字}}]}。

foot_anchor 必须始终是同时包含 x 和 y 的 JSON 对象，绝不能是单个数字、数组、字符串、null 或其他结构。正确示例："foot_anchor":{"x":177.5,"y":340}。

最终发送前，删除 JSON 对象前后的全部文字。只返回一个完整、准确、可解析的 JSON 对象，不要使用 Markdown 代码块，不要添加解释、标题、注释、尾随逗号或任何 JSON 之外的字符。

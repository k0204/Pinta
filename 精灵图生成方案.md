# 精灵图生成方案

## 1. 目标与范围

本方案把 AI 生成的方向图、动作图、拆分、命名、抠图和图层归档定义成一个连续的、可重复的工作流。生成结果始终写入当前 Pinta 文档的图层树，不再通过 `NewDocumentFromImage` 打开新文档。

本阶段要解决的事情：

- 建立名为 `spritesheet` 的根图层。
- 在根图层下按动作维护多个动作组。
- 每次生成保留一个独立的尝试组，避免重试覆盖旧结果。
- 允许选择生成类型、动作、方向和每方向帧数。
- 保留完整方向图作为中间结果和重拆分源。
- 由用户显式确认拆分参数和命名，再创建方向序列帧图层。
- 由用户显式触发抠图；抠图失败时可以从原始帧再次尝试。
- 明确每个切片如何映射到方向和帧，并放入对应图层。

本阶段不做：

- 全自动抠图。
- 根据图片内容猜测方向、动作或帧边界。
- 自动删除原图、旧尝试或失败结果。
- 运行时动画播放器、Spine 导出器或游戏引擎资源导出器。

## 2. 已确认的产品决策

### 2.1 方向图是否保留

保留。方向图不是一次性临时文件，而是以下工作的唯一可追溯源：

1. 检查 AI 是否按方向和顺序生成。
2. 调整列数、行数、边距或帧尺寸后重复拆分。
3. 在抠图失败时重新生成透明帧。
4. 对比多个生成尝试并选择较好的方向。
5. 在项目保存后继续处理。

因此完整方向图必须作为 `source-sheet` 普通图层保留，并且默认不被拆分或抠图操作破坏。

### 2.2 生成方向

对话框按配置提供八个标准方向，方向 ID 使用稳定值，显示名称可本地化：

`down`、`down-left`、`left`、`up-left`、`up`、`up-right`、`right`、`down-right`。

用户可以勾选任意一个或多个方向。方向图模式默认全选；动作模式默认选择前、左、后、右四个主方向，但用户可以改为任意组合。未选择的方向不生成、不占用网格单元，也不创建空方向组。

发送请求前必须校验至少选择一个方向，并在摘要中显示方向数量和顺序。

### 2.3 动作与帧数

动作模式选择一个动作预设（`idle`、`walk`、`run`、`jump`、`attack`、`hurt`、`death` 或 `custom`），并设置“每方向帧数”。

- 第一版范围：1 到 16 帧，步长 1。
- 每个已选方向使用相同帧数。
- 总帧数 = 已选方向数 × 每方向帧数。
- 方向图模式固定每方向 1 帧，帧数控件隐藏或禁用。
- 切换动作时只重置为该动作的默认帧数，不改变用户已经勾选的方向，除非用户切换生成类型。

如果以后需要每个方向不同帧数，应扩展为方向级帧数表；本阶段不引入该复杂度。

## 3. 权威图层结构

所有精灵图内容使用现有树形图层 API（`DocumentLayers`、`GroupLayer`、`UserLayer`），不再引入平行的扁平列表模型。

```text
spritesheet                         GroupLayer，项目唯一根节点
└── walk                            GroupLayer，动作组，稳定动作 ID
    ├── attempt-01                  GroupLayer，一次生成/拆分尝试
    │   ├── source-sheet            UserLayer，完整 AI 方向图，永不自动删除
    │   ├── down                    GroupLayer，方向序列帧组
    │   │   ├── frame-01            UserLayer，原始切片
    │   │   ├── frame-02            UserLayer，原始切片
    │   │   └── ...
    │   ├── left                    GroupLayer
    │   │   └── frame-01 ...
    │   └── ...
    └── attempt-02                  GroupLayer，重试结果，与 attempt-01 并存
```

### 3.1 为什么需要 `attempt`

动作组直接放方向组无法区分多次生成或重新拆分的来源。`attempt` 是最小的版本边界：它同时保存生成参数、原图和由该原图拆出的帧。重新生成或使用新网格重新拆分时创建新的 `attempt-02`，不覆盖 `attempt-01`。用户可以隐藏、比较或删除整个尝试组。

从用户角度看，动作仍然拥有方向序列帧；`attempt` 只是把“这批帧来自哪一张方向图”明确保存下来。

### 3.2 命名规则

- 根组：`spritesheet`，如果文档中已有同名组则复用，不重复创建。
- 动作组：稳定 ID 优先，例如 `walk`；显示名可为“行走”，但不能丢失 ID。
- 尝试组：`attempt-01`、`attempt-02`，按动作组内顺序递增。
- 源图层：固定为 `source-sheet`；若同一尝试需要保留重新导入的源图，使用 `source-sheet-02`，不覆盖已有源图。
- 方向组：使用方向 ID，例如 `down-left`。
- 帧图层：两位序号 `frame-01` 至 `frame-16`，按方向内从 1 开始。
- 抠图结果：在原始帧旁创建 `frame-01-cutout-01`；每次失败重试递增序号，原始帧永远保留。

用户可以在提交拆分前修改显示名称，但内部映射仍使用方向 ID、帧索引和单元索引保存。

## 4. 从生成方向图到最终帧的完整操作

### 第一步：打开生成面板

从图层工具栏启动精灵图生成。读取当前活动文档作为目标文档；没有活动文档时禁用该命令。生成开始前不创建新文档。

面板必须显示：

- 生成类型：方向图 / 动作序列。
- AI 服务、GPT provider、输出尺寸、背景色和可编辑提示词。
- 方向勾选项及其固定顺序。
- 动作预设；选择 `custom` 时显示自定义动作描述。
- 动作模式下的每方向帧数。
- 根据方向数、帧数和输出尺寸计算的总帧数、列数、行数。

### 第二步：确认请求参数

点击生成前执行以下校验：

1. 至少选择一个方向。
2. 动作模式必须有动作 ID；自定义动作必须有非空描述。
3. 帧数在 1 到 16 之间。
4. 输出尺寸满足所选 AI 服务限制。
5. 提示词非空。

生成请求模型必须携带 `GenerationType`、`ActionId`、`SelectedDirectionIds`、`FrameCount`、`Columns`、`Rows`、`ImageSize`、`BackgroundId` 和最终提示词，不能只传一个结果图层名称。

### 第三步：请求与重试

继续复用统一的 `/api/images` 和轮询逻辑，通过 `provider` 选择上游。保留当前取消、进度、余额刷新和调试日志行为。

网络或服务失败时只结束当前请求，不创建空尝试组。成功收到 PNG 后才创建图层树。服务返回尺寸异常时沿用现有校正策略，校正后的尺寸必须写入尝试元数据。

### 第四步：把结果插入当前文档

生成成功后：

1. 找到或创建根 `spritesheet` 组。
2. 找到或创建对应动作组。
3. 在动作组内创建下一个 `attempt-NN` 组。
4. 将完整 PNG 绘制到 `source-sheet` 图层。
5. 将尝试设为当前选中图层，并展开该组。
6. 保持当前文档为活动文档，绝不调用 `workspace.NewDocumentFromImage`。
7. 将本次请求参数与网格信息保存到尝试元数据，供后续重新拆分使用。

此时流程停止，用户先检查方向图；系统不自动拆分、不自动抠图。

### 第五步：检查方向图

用户在画布和图层面板中检查：

- 角色是否保持一致。
- 方向顺序是否与提示词一致。
- 每个网格单元是否只有一个角色。
- 是否存在空单元、串格、裁切或背景颜色错误。

检查不通过时，用户可以直接删除当前尝试组或保留它并再次生成新的尝试。旧尝试不被覆盖。

## 5. 拆分设计

### 5.1 拆分入口

对 `source-sheet` 图层提供“拆分精灵图”命令。命令只对精灵图尝试源图有效，不对普通图层猜测网格。

首次打开拆分面板时读取生成元数据；若元数据不存在，要求用户输入：

- 列数、行数。
- 单元宽、单元高。
- 左/上起始偏移。
- 横向、纵向间距（默认 0）。
- 是否跳过空单元。

面板提供编号预览，编号从左上角开始，按行优先（row-major）递增。用户必须在提交前确认预览，不进行内容识别。

### 5.2 单元索引到方向/帧的确定映射

生成提示词已经规定了固定的行优先顺序，拆分必须使用同一规则：

- 方向图模式：第 `i` 个选中方向对应第 `i` 个单元。
- 动作模式：第 `i` 个方向占用连续 `FrameCount` 个单元。
- 方向排序使用请求中的 `SelectedDirectionIds` 顺序，而不是当前 UI 语言或图层排序。

令 `F` 为每方向帧数，`d` 为方向在选中列表中的从 0 开始索引，`f` 为帧索引，则：

```text
cellIndex = d × F + f                 // 从 0 开始
column = cellIndex % columns
row = cellIndex / columns
sourceRect = (offsetX + column × (cellWidth + gapX),
              offsetY + row × (cellHeight + gapY),
              cellWidth, cellHeight)
```

越过 `totalFrames` 的空单元不创建图层；如果用户取消“跳过空单元”，则创建空的占位帧并明确标记为 `empty-frame-NN`。

### 5.3 拆分提交后如何放入对应图层

拆分提交必须是一次可撤销的树操作，具体步骤如下：

1. 在当前 `attempt-NN` 下，为每个选中的方向创建或定位方向 `GroupLayer`。
2. 按方向顺序遍历帧索引，使用上面的 `sourceRect` 从 `source-sheet` 复制像素到新 `UserLayer`。
3. 新图层命名为 `frame-01`、`frame-02`，并按帧索引排序插入方向组。
4. 只写入当前尝试组，不把帧散落到文档根层或其他动作组。
5. 如果目标方向组已有同名帧，弹出覆盖确认：默认复制 `source-sheet` 和元数据到新的 `attempt-NN` 后写入，不静默覆盖旧帧。
6. 将最后一个创建的帧设为当前图层，方便用户马上执行抠图。
7. 记录一个包含源图、矩形、目标方向和帧索引的历史项，使整次拆分可以一次撤销。

拆分不是“打开切片文件再导入”，而是直接在当前文档中创建图层。每个帧图层的尺寸使用单元尺寸，位置默认归零；这样帧层本身就是可复用的精灵素材，而不是仍然占据整张画布的透明大图。

### 5.4 重新拆分

用户可以修改列、行、尺寸或间距后再次执行拆分。再次提交时：

- 默认在同一动作下创建新的 `attempt-NN`，复制同一 `source-sheet` 并记录它来自哪个尝试。
- 用户确认“替换当前拆分”时，仅替换当前尝试中选定方向的帧组。
- `source-sheet` 永不修改。
- 任何旧切片只有在用户明确删除时才删除。

## 6. 抠图流程

### 6.1 抠图发生在哪一步

抠图发生在拆分之后、逐帧确认之后。生成阶段只负责得到方向图；拆分阶段只负责按矩形复制像素；抠图阶段才处理背景透明化。

禁止把抠图逻辑塞入 AI 生成完成回调，也禁止在拆分时隐式调用抠图服务。这样用户可以在不消耗额外额度的情况下反复调整拆分参数。

### 6.2 单帧抠图操作

用户选中一个原始 `frame-NN` 图层后点击抠图：

1. 打开现有 AI 抠图设置，用户选择 Agnes、GPT Image 或 Baidu。
2. 用户确认提示词、参考图和服务参数。
3. 请求只发送当前帧，不发送整张方向图。
4. 成功时在同一方向组中创建 `frame-NN-cutout-01`。
5. 原始 `frame-NN` 保持不变并默认隐藏或由用户手动切换可见性。
6. 失败时保留原始帧，显示错误，可再次点击抠图生成 `cutout-02`。

### 6.3 批量抠图（第二阶段可选）

批量抠图只能是用户明确选择的动作、方向或帧范围后执行；它仍然逐帧产生独立结果，不能把失败帧吞掉。第一版可以先只实现单帧，避免一次请求失败导致整批结果不可追溯。

## 7. 重试与结果选择

### 7.1 生成重试

同一动作再次生成时总是创建新的 `attempt-NN`。新尝试继承本次面板参数的初始值，但不自动删除旧尝试。用户可以：

- 隐藏旧尝试以比较新结果。
- 仅对新尝试拆分。
- 从旧尝试复制某个方向组到另一个尝试。
- 删除整个尝试组（普通图层删除历史可恢复）。

### 7.2 拆分重试

同一源图的不同网格参数生成新的 `attempt-NN`，新尝试复制源图并保存列、行、尺寸、间距和跳过空单元选项。用户选择某个尝试作为当前工作版本，不需要重新调用 AI。

### 7.3 抠图重试

每次抠图尝试都创建带序号的新结果层；成功结果不覆盖失败结果。用户通过隐藏/显示对比，最终可手动重命名一个结果为 `frame-NN-final`。后续导出只读取用户明确标记的 final 层，第一版不自动推断“最好”的结果。

## 8. 实现任务拆分

### P0：模型与流程边界

1. 在 `LayerActions.Background.cs` 中把现有 `AiImageRequestOptions` 扩展为包含精灵图专用字段：生成类型、动作 ID、选中方向 ID、帧数、列/行、背景 ID、源尺寸和提示词版本。
2. 保持普通 AI 生图和背景清理的现有行为，只让 `SpritesheetGeneration` 进入新流程。
3. 定义不可变的 `SpritesheetAttemptInfo`，作为生成、拆分和 UI 之间的唯一参数对象。
4. 明确 `totalFrames`、`columns`、`rows` 的计算入口，复用 `SpritesheetPromptCatalog.CalculateGrid`，避免 UI 和执行层各算一套。

完成标准：生成请求返回的数据足以在不重新打开对话框的情况下完成拆分；普通 AI 生图没有回归。

### P1：生成面板

1. 保留现有方向选择、动作预设、背景、服务和提示词控件。
2. 增加明确的方向顺序摘要，例如“down, left, up, right”。
3. 增加帧数边界校验和总帧数/网格摘要。
4. 方向图模式固定 `FrameCount = 1`；动作模式显示 1 至 16 的帧数控件。
5. 将最终提示词和结构化参数一起提交，避免只从已编辑文本反推配置。

完成标准：用户可以明确知道将生成哪些方向、每个方向多少帧，以及拆分时单元如何编号。

### P2：当前文档内插入图层树

1. 添加 `FindOrCreateSpritesheetRoot`、`FindOrCreateActionGroup` 等最小辅助函数，全部基于 `DocumentLayers` 的树 API。
2. 用 `CreateGroupLayer`、`CreateLayer` 和 `Insert` 建立 `spritesheet/action/attempt` 结构。
3. 将 PNG 写入 `source-sheet`，并保持源图完整。
4. 加入一次“新增尝试”历史记录；插入失败时清理未完成的临时节点。
5. 删除 `GenerateImageAsync` 中针对精灵图的 `NewDocumentFromImage` 分支；普通 AI 生图仍按原有方式新建文档。

完成标准：生成完成后活动文档不变，图层面板能看到完整的尝试树，关闭并重新打开项目后源图仍存在。

### P3：尝试元数据持久化

1. 在尝试组上保存动作 ID、方向 ID 顺序、帧数、列、行、单元尺寸、间距、偏移、背景 ID、服务和提示词。
2. 优先扩展现有 Pinta 文档格式的文档/图层扩展字段；不要另建与 Pinta 项目脱离的必需旁车文件。
3. 读取旧项目时元数据缺失必须进入“手动输入拆分参数”流程，不猜测。
4. 元数据写入失败不能阻止源图保存，但必须在 UI 中提示“重新打开后需要手动设置网格”。

完成标准：项目重开后可以直接对源图执行拆分，且方向和帧映射与生成时一致。

### P4：拆分面板与树操作

1. 实现只针对 `source-sheet` 的拆分命令。
2. 显示列/行、单元宽高、偏移、间距和跳过空单元控件。
3. 提供按行编号预览和可编辑方向/帧命名表。
4. 使用确定性的 `cellIndex -> direction/frame -> sourceRect` 映射。
5. 将每个切片复制到目标尺寸的新 `UserLayer`，放入当前 `attempt` 的方向组。
6. 整次拆分使用一个复合历史项，支持一次撤销/重做。
7. 提供“创建新尝试”和“替换选中方向”两个明确按钮，默认不覆盖。

完成标准：用同一张源图重复拆分两次可以并存；任意帧都能从图层路径反查方向、帧号和源单元号。

### P5：抠图接入

1. 复用当前图层抠图设置和三种服务，不复制 API 客户端。
2. 将输入限制为一个普通帧图层；GroupLayer、source-sheet 和缺失引用层不可直接抠图。
3. 结果新建为带序号的 cutout 层，保留原帧层。
4. 失败、取消、余额刷新和调试日志沿用现有行为。
5. 在图层面板提供原始帧/抠图结果的可见性切换。

完成标准：抠图失败可以重复尝试；任何一次尝试都不会破坏源图或原始帧。

### P6：复制、选择与清理

1. 允许从一个尝试复制整组方向或单个方向到另一个尝试。
2. 复制时保持帧顺序和名称，遇到冲突要求用户选择追加或替换。
3. 删除动作、尝试和抠图结果全部走现有历史删除逻辑。
4. 删除前给出明确层路径，防止误删整个 `spritesheet` 根组。

完成标准：用户能组合不同尝试中的优质方向，同时保留每个来源的追溯关系。

### P7：文档与手工验收

1. 更新 `doc/api.md` 中“生成的图片打开为新文档”的旧描述，改为“精灵图插入当前文档图层树”；记录新增请求字段和拆分语义。
2. 不新增测试文件；使用现有构建和手工检查验证。
3. 编译前停止 Pinta 和 `.NET Host` 进程，避免锁住输出文件。
4. 手工验收至少覆盖：
   - 4 方向 × 4 帧动作图。
   - 8 方向 × 1 帧方向图。
   - 单方向、16 帧和自定义动作。
   - 空单元、非零偏移和有间距的手动拆分。
   - 生成重试、拆分重试、抠图重试。
   - 保存、关闭、重开后继续拆分。
   - 取消和服务失败不产生半截图层树。

## 9. 关键错误处理

| 场景 | 行为 |
| --- | --- |
| 未选方向 | 禁用生成并提示至少选择一个方向 |
| 自定义动作为空 | 禁用生成 |
| AI 请求失败 | 不创建尝试组；保留日志和错误信息 |
| PNG 解码失败 | 不插入图层；当前文档不变 |
| 网格超出源图 | 禁止拆分提交，标出越界矩形 |
| 目标方向已有帧 | 默认创建新尝试，禁止静默覆盖 |
| 单帧抠图失败 | 保留原始帧，允许生成下一个 cutout 尝试 |
| 元数据不存在 | 进入手动网格参数流程，不猜测方向或帧 |
| 用户取消 | 不提交未完成树变更，已存在的旧尝试不受影响 |

## 10. 当前提示词评估

当前提示词来源为 `config/spritesheet-prompts`。结论是：**能够约束单次方向图或动作图的基本一致性，但不足以保证不同动作、不同请求之间的角色与方向统一性。** 提示词只能提高成功率，跨请求一致性还必须由固定参考图、明确的参考图角色和结果验收共同保证。

### 10.1 已经满足的部分

- `common.json` 已要求角色身份、脸、发型、服装、配件、色板、轮廓、比例、缩放和渲染风格在同一张图的所有单元中保持一致。
- 八方向具有稳定 ID、固定顺序和明确朝向描述。
- `direction-sheet.json` 已把方向图定义为后续动画使用的 canonical turnaround，而不是动作帧。
- 动作模板覆盖循环、非循环、关键姿势、武器连续性和禁止新增内容等基本要求。
- `SpritesheetPromptCatalog.BuildPrompt` 已写明行优先单元编号、每方向连续帧区间、网格尺寸和空单元规则。

### 10.2 仍然存在的缺口和冲突

| 问题 | 当前表现 | 必须补充的约束 |
| --- | --- | --- |
| 跨动作身份漂移 | 只说“有参考图时”保持一致，没有规定每次动作生成必须使用哪张参考图 | 每个动作请求都必须使用同一份已确认的 canonical 方向图作为第一身份锚点；没有锚点时只允许标记为草稿 |
| 方向造型漂移 | 方向文本只描述朝向，未锁定不对称服装、武器手、肩甲、发缝等左右关系 | 明确保持角色自身的左/右侧语义，不得在方向间交换配件、持武器手或服装细节 |
| 跨方向动作相位不统一 | 每个方向有相同帧数，但没有规定同一帧号必须处于同一动作相位 | 规定所有方向的 `frame-f` 使用同一个归一化相位；例如每个方向的 frame-03 都是同一接触/挥击阶段 |
| 任意帧数与动作模板冲突 | `walk/run/jump` 固定列出 8 个阶段，UI 却允许 1 到 16 帧 | 动作模板保存关键姿势计划，由构建器根据实际帧数重采样；不得把 8 个阶段原样套到任意帧数 |
| 相机约束含义模糊 | `camera angle` 固定容易与八方向角色旋转冲突 | 固定的是投影方式、俯视角、镜头、画布构图和缩放；变化的是角色绕竖直轴的朝向 |
| root 约束与跳跃冲突 | 通用规则要求脚/root 固定，`jump` 又要求表现垂直位移 | 落地动作锁定地面锚点；跳跃允许身体相对地面锚点垂直移动，但单元原点、相机和缩放不变 |
| 参考图角色不明确 | 请求可以附加多个普通图层或文件，文件名不能说明用途 | 用稳定文件名和提示词声明 `character-anchor`、各方向 anchor、可选 motion reference；禁止把未确认的生成结果当作唯一身份锚点 |
| 大图容量未约束 | 最多可请求 8 方向 × 16 帧，单格可能过小 | 提交前计算单元像素尺寸并设置最低阈值；超过阈值时减少方向/帧或拆成多次请求，但每次复用同一 canonical 锚点 |
| 背景色可能污染角色 | 只要求纯色背景，没有禁止角色使用键色 | 色键模式必须禁止角色、配件和边缘高光使用所选键色；与角色冲突时切换键色 |

### 10.3 推荐的一致性生成契约

第一版不需要增加新的生成模型或依赖，只需把现有“方向图 -> 动作图”变成强制的两阶段流程：

1. 先使用角色原图生成八方向 canonical 方向图，用户确认身份、比例、不对称细节和方向后，将该尝试标记为 `character-anchor`。
2. 后续 `idle`、`walk`、`run`、`attack` 等每一次请求，都把同一个 `character-anchor` 作为第一参考图，不允许只引用上一次动作结果，以免误差逐次累积。
3. 提示词按角色身份、相机、方向映射、动作相位、网格、背景和禁止项的固定顺序构建。参考图必须在提示词中按文件名说明用途。
4. 每个方向使用同一套相位表。令每方向帧数为 `F`，第 `f` 帧的归一化相位为 `f / F`（循环动作）或 `f / (F - 1)`（非循环动作，`F = 1` 时取关键代表姿势）。
5. 生成后至少检查角色身份、方向准确度、同帧跨方向相位、包围盒/脚点漂移、键色污染和单元越界。未通过的尝试保留，但不能被选为后续身份锚点。

建议将通用提示词的核心语义改为：

```text
Reference roles:
- character-anchor: the approved canonical turnaround for this exact character; it is the authoritative identity, costume, palette, proportions, asymmetry, and direction reference.

Identity lock:
Use character-anchor as a strict identity constraint, not as loose inspiration. Do not redesign or simplify the character. Preserve the character's own left/right anatomy and all asymmetric details across views; never swap the weapon hand, accessories, hair part, armor, markings, or costume sides.

Camera and registration lock:
Keep the projection, camera elevation, lens, framing, cell origin, character scale, and rendering style fixed. Rotate only the character to the requested direction. For grounded actions, keep the ground contact anchor fixed. For airborne actions, keep the ground anchor and camera fixed while allowing the body to move vertically inside the cell.

Cross-direction phase lock:
For every frame index, all selected directions must depict the same normalized animation phase and the same semantic key pose. Change only the viewing direction. Keep timing, contact state, body mechanics, held objects, and action intensity equivalent across directions.
```

动作模板不应再假设恒定 8 帧。每个动作配置应保存关键姿势顺序，最终提示词根据 `FrameCount` 输出明确的逐帧计划。例如 4 帧 walk 使用 `contact -> passing -> opposite contact -> opposite passing`；8 帧再使用当前完整八相位；其他帧数由同一相位序列均匀采样。`idle`、`walk`、`run` 属于循环动作，末帧应自然过渡到首帧但不要复制首帧；`jump`、`attack`、`hurt`、`death` 默认是非循环动作，首尾必须表达明确的开始和结束状态。

## 11. GitHub 开源项目调研

调研时间为 2026-07-29。公开搜索中未发现一个成熟项目同时满足“Codex 工作流 + GPT Image + 八方向多动作 + Pinta 图层树”；以下项目分别覆盖其中一部分，适合借鉴具体做法，不适合整体照搬：

| 项目 | 与本方案的关系 | 值得借鉴 | 不应照搬 |
| --- | --- | --- | --- |
| [ianlintner/ai-pixel-art-image-generation](https://github.com/ianlintner/ai-pixel-art-image-generation) | 使用 `gpt-image-2` 生成基础精灵，并生成动画图集 | 第一帧作为后续帧参考；整组只采样一次风格；记录最终提示词与参数；用 silhouette IoU、包围盒漂移等指标做确定性 QA | 动画帧使用 Gemini 继续生成，是混合模型流程，并非纯 GPT Image 或 Pinta 工作流 |
| [MeowAI-HK/gpt-image-sticker-generator](https://github.com/MeowAI-HK/gpt-image-sticker-generator) | 使用 `gpt-image-2` edit 和固定 5×5 控制网格生成连续 25 帧 | 把网格模板作为输入图；固定行优先顺序；确定性切片；保存源图、帧预览表和动画预览 | 只处理单方向贴纸动画，没有跨方向、跨动作身份管理 |
| [pickbitsai/sprite-generator](https://github.com/pickbitsai/sprite-generator) | OpenAI 图像模型驱动的 manifest 精灵生成器，支持 angles、animations 和 frame descriptions | 权威 manifest、逐帧语义、dry-run、稳定元数据、身份相似度检查和图集组装 | 每个方向/帧独立请求，调用量大；仅靠文本前缀不能达到本方案要求的强身份锁定 |
| [GAlbanese09/spritebrew](https://github.com/GAlbanese09/spritebrew) | 完整的 AI 精灵生成、切片、预览和多引擎导出产品 | 上传角色后生成动作、自动预处理、网格预览、方向分组、人工像素修正和导出体验 | 使用 Retro Diffusion，不是 GPT Image；产品资产模型不替代 Pinta 图层树 |
| [Allen-Saji/spriteforge](https://github.com/Allen-Saji/spriteforge) | 使用参考角色生成多姿势精灵 | IP-Adapter 锁定角色、ControlNet/OpenPose 控制姿势，说明“身份参考”和“姿势参考”应分离 | Stable Diffusion/CUDA 管线较重，不应为第一版引入这些依赖 |

其中最值得直接吸收的是三点：固定网格输入图、永远复用同一个 canonical 角色锚点、在生成后执行可量化的一致性检查。Pinta 现有 API 已支持多个 `reference_files`，因此第一版先把参考图角色和提示词契约写清，不需要为此更换服务或增加生成框架。

## 12. 与参考项目的借鉴边界

`G:\AISpine\gorest-2d-animation-spritesheet-generator-main\gorest-2d-animation-spritesheet-generator-main\src` 中的以下思路可借鉴：

- `SpritesheetImporterPanel.tsx`：导入时明确帧数、列数、帧宽高，并提供“根据图集推断尺寸”的显式操作。
- `assetSpriteOperations.ts`：网格重建时校验列数、帧尺寸、帧数是否超出源图，并保留帧元数据。
- `startUiAssetNaming.ts`：名称清洗和稳定文件名的思路可用于动作、方向和帧名称。

不直接照搬其资产模型、运行时绑定、自动导入或自动下载流程。Pinta 的权威数据是当前文档中的图层树，所有生成和拆分结果必须服务于该树。

## 13. 第一版交付判定

只有以下条件全部满足，才认为精灵图功能完成：

1. 生成结果进入当前文档，不再打开新文档。
2. 图层树存在 `spritesheet/action/attempt/source-sheet`。
3. 用户能选择方向和每方向帧数，提示词与实际映射一致。
4. 方向图可以保留并重复拆分。
5. 拆分结果按确定的行优先规则进入对应方向组和帧图层。
6. 抠图是用户触发的独立步骤，且每次结果可保留、可比较、可重试。
7. 生成、拆分和抠图的失败或取消不会破坏已有结果。
8. 文档保存并重开后，结构和拆分参数仍可继续使用。
9. 所有动作尝试都能追溯到同一个已确认的 canonical 角色锚点，同帧号在各方向表达同一动作相位。

## 14. 推荐实施顺序

按 `P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7` 实施。P2 完成后即可验证“生成图成为新图层”的核心诉求；P4 完成后即可验证“拆分后如何放入对应图层”；P5 再加入抠图，不让抠图逻辑阻塞生成和拆分主链路。

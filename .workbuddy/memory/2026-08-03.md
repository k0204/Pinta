# 2026-08-03

## 修复图层预览弹窗（Ctrl+左键）非等比缩放

- 文件：`Pinta.Gui.Widgets/Widgets/Layers/LayerPreviewWindow.cs`
- 根因：`preview_area`（Gtk.Overlay）直接作为 `Gtk.ScrolledWindow` 的子控件时，GTK4 会用 GtkViewport 包裹并把子控件拉伸到**至少视口大小**。缩小到小于视口时 overlay 被拉伸，`Gtk.Picture` 的 `ContentFit.Fill` 随之把图像拉成视口宽高比 → 变形。放大超过视口时分配==请求，所以看起来正常，导致"缩放逻辑不对"的表象。
- 修复：overlay 外层包一个 `Gtk.Box` 作为滚动子控件；overlay 设 `Hexpand/Vexpand=true` + `Halign/Valign=Center`。这样 overlay 始终按 `SetSizeRequest` 请求的等比尺寸分配并居中，不再被视口拉伸。
- 要点：GTK4 中 expand=true + align=Center 的组合表示"拿满槽位空间、内容按 natural 尺寸居中"，是视口内居中控件的标准写法。
- 验证：无残留 Pinta 进程；`dotnet build Pinta.Gui.Widgets -c Release` 通过。

## 预览窗口打开时视图居中

- 需求：窗口打开时显示图像中心，而不是左上角。
- v1：在 Configure 的 IdleAdd 里调 CenterView()。风险：Windows 上 GTK4 帧时钟/布局可能晚于 idle 执行，滚动条 Upper 还没配置 → 居中静默失败（停在左上角）。
- v2（最终）：改为 `CenterViewWhenReady()` —— 订阅 H/V 两个 `Gtk.Adjustment.OnChanged`，当 `Upper>1`（布局已配置边界）后居中一次并退订；订阅后立即调用一次以覆盖"已就绪"情况。无时序竞态，确定性。
- 居中公式：`SetAdjustmentValue(adj, (adj.Upper - adj.PageSize) / 2)`，复用已有的 clamp 逻辑；内容小于视口时自然落到 0（由 Box 包裹居中）。
- 运行方式：用户约定直接用 `./run.bat`（= dev.ps1 -Run，Debug 构建到 build/bin 后启动 Pinta）。
- 环境注意：`Pinta.Resources\bin\Release\net10.0\icons\...` 的 PNG 曾被未知进程锁定导致 Release 构建 MSB3021 失败（Access denied）；Debug 构建到 build/bin 不受影响。构建前按 AGENTS.md 先停 Pinta/.NET 进程。

## v3（最终交付版）：居中改为"已知图像尺寸 + 多路触发"

- 上一版 `CenterViewWhenReady`（Upper>1 检查 + 订阅 OnChanged）用户实测仍停左上角；且由于构建锁问题用户跑到的多为旧二进制，v2/v3 均未被真正验证。
- v3 逻辑（当前代码状态，未编译验证）：
  - `TryCenterViewOnce()`：内容尺寸直接用 `GetScaledSize(preview_texture.Width/Height)`（用户建议"获得图像大小后进行居中"），偏移 = `(content - PageSize)/2`；就绪条件改为 `PageSize>0`（视口已布局）；`view_centered` 字段幂等去重。
  - 触发源 4 个：adjustment H/V `OnChanged`、窗口 `OnMap`+idle、Configure 的 IdleAdd、立即调用一次——谁先观察到布局完成谁执行。
  - 保留 `[LPV]` stderr 诊断日志（验证 OK 后移除）。
- 锁问题诊断结论：失败构建残留的 MSBuild 节点（dotnet.exe /nodemode:1）持有输出文件句柄 → 后续构建连锁 Access denied（MSB3021 随机文件）；杀掉全部 dotnet 进程后瞬时可解。handle64 无管理员权限看不到持有者。
- **用户明确要求：只改代码，不要自动编译。** 已写入 MEMORY.md。

## v4（当前）：调整值对象接错——改为从自动 viewport 取

- 用户第三次反馈打开仍左上角。v3 逻辑（TryCenterViewOnce + 多路触发）理论上正确但实际不生效。
- 真正根因：`Gtk.ScrolledWindow` 对非 scrollable 子控件会**自动包一个 Gtk.Viewport**，该 viewport 用的是自己创建的 Gtk.Adjustment（`gtk_viewport_new(priv->hadjustment, priv->vadjustment)`——若 priv->hadjustment 此时为 NULL，则 viewport 自行创建，scrolled window 的 priv 保持 NULL）。之后 `scrolled_window.Hadjustment` getter 是懒构造的——会创建一个**全新的、独立的** adjustment 存进 priv，但这个对象**从未与 viewport 关联**，viewport 继续用自己那套。因此 `scrolled_window.Hadjustment` 在"非 scrollable 子+自动 viewport"场景下返回的是**死对象**：
  - 订阅 `OnChanged` 从不触发（无人 configure 这个对象）。
  - `PageSize` 永远 0，居中条件 `PageSize > 0` 永不满足。
  - `SetScale` 写入死对象，缩放时滚动条不动（用户说的"缩放后居中"实际只是缩小到 < 视口时 Box 包裹的视觉居中）。
  - 拖拽平移同理不生效。
- 修复（参照 `CanvasWindow.cs` / `DocumentWorkspace.cs` 的成熟模式）：添加 `RealHorizontal`/`RealVertical` 两个属性，从 `scrolled_window.Child`（自动 viewport）调用 `GetHadjustment()`/`GetVadjustment()` 拿到真正被配置的 adjustment。替换全部 10 处 `scrolled_window.Hadjustment!`/`Vadjustment!` 引用（含 SetScale 与 HandlePanBegin/Update）。
- 启示：在 GTK4 中，`GtkScrolledWindow.Hadjustment` 对**非 scrollable 子（自动 viewport）**不可靠，对**显式 scrollable 子（如 Gtk.Viewport）**才等价于子自己的 adjustment。需要直接拿子（viewport）的 adjustment。

## v5（当前）：一次性标志 → 持续居中直到用户交互

- 用户第四次反馈：打开仍不居中，但移动/缩放后就居中了。说明 v4 的 `RealHorizontal/Vertical` 修复生效（交互时写入能改变 scroll），但**打开时的首次居中没成功**。
- 根因：v4 用 `view_centered` 一次性标志——首次 `OnChanged`（PageSize>0）时居中并设 true。但**首次 OnChanged 的 PageSize 可能是窗口未最终确定尺寸时的临时值**，居中到错误位置；之后窗口尺寸确定、PageSize 变为最终值、OnChanged 再次触发——但 `view_centered` 已 true，不再居中。用户交互（缩放/移动）触发了**重新布局** → OnChanged 再次触发 → 此时 view_centered 仍是 false（因为之前的 OnChanged PageSize=0 没设 true）→ 居中成功。
- 修复：把 `view_centered`（一次性）改为 `user_interacted`（语义反转）——**用户交互前每次 OnChanged 都重新居中**（即使首次 PageSize 是临时值，后续 PageSize 确定后也会重新居中到正确值）；用户一旦滚轮缩放/拖拽平移/键盘缩放就设 `user_interacted = true`，之后不再自动居中，保留用户位置。
- 改动点：字段 `view_centered` → `user_interacted`；`TryCenterViewOnce` → `CenterView`（去掉一次性检查）；OnChanged handler 加 `if (!user_interacted)` 守卫；HandleScroll/HandlePanBegin/HandleKeyPressed 设 `user_interacted = true`。

## Create Animation Frames 对话框布局重排（三栏）

- 用户选择"全面重排三栏结构"方向。原布局问题：中列 源预览(260) + 标题 + 帧预览(340+标尺24) + 工具栏 ≈ 738px，超过 780px 窗口可用高度，内容被挤压；校验信息埋在设置列底部不易发现。
- 新布局：左设置列(280) | 中列仅帧预览+标尺（工具栏置顶，吃满全高） | 右列(300)=源预览(固定200，作概览)+帧列表(扩展)+X/Y 位置；校验信息改为底部通栏状态栏（分隔线+label，Error 样式逻辑不变）。
- 代码组织：新建 `Pinta.Core/Actions/SpritesheetSplitDialog.Layout.cs`，集中全部 Build*/Create* 布局方法（从主文件/Analysis/Guides 迁入），主文件只留构造+逻辑。符合 AGENTS.md partial-class 约定（TypeName.FeatureName.cs）。
- `CreatePreview` 不再默认 Vexpand（源预览固定高、帧预览构造里显式 `Vexpand=true`）；工具栏加 `AdwaitaStyles.Toolbar` 样式类。
- 未编译（按约定用户自己用 ./run.bat 验证）。Pinta.Core 为 SDK 风格工程，新 .cs 自动包含。

## Create Animation Frames：全屏按钮 + root 移动 + 高级项折叠

- **全屏按钮**：构造函数里 `Gtk.HeaderBar` + `PackEnd`（图标 `Resources.StandardIcons.ViewFullscreen`）+ `dialog.SetTitlebar`，模式照搬 `LayerPreviewWindow.cs`；`ToggleFullscreen()` 用 `fullscreened` bool 切换 `dialog.Fullscreen()/Unfullscreen()`。
- **root 移动勾选框**（`move_root`，右列 Frame position 下）：勾选 → `root_dy=-30, root_dx=0`；取消 → 归零。`RepositionFramesAroundAnchor` 改为 root = (canvas_w/2+root_dx, canvas_h+root_dy)，`DrawAnchorMarker` 同点。勾选后可在帧预览中拖拽红色锚点十字（命中容差 10px，优先级：guide > root > frame；`DragRoot` clamp 在画布范围内）。root 拖拽不进位置 undo 历史（PositionHistory 仅逐帧）。
- **Grid 页高级项折叠**：Left/Top offset、H/V gap 收入默认折叠的 `Gtk.Expander("Offsets and gaps")`；Columns/Rows/Cell 宽高保持直显（切分必需参数）。
- 注意：`Gtk.HeaderBar` 默认 `show-title-buttons=true`，SetTitlebar 后关闭按钮仍在。

## Create Animation Frames：三列改两列 + 预览 tab + 状态隐藏

- 三列 → 两列：左列 320px = 设置区（Source extraction tabs、Output canvas、Output attempt）+ 帧管理区（帧列表、X/Y、move_root、hint）；右列全宽 = 工具栏 + 预览区。预览宽度从 ~580px 增至 ~820px。
- 预览区改 tab：`preview_stack`（Adw.ViewStack）含 "Source"（源图全尺寸）/ "Frame"（标尺+帧预览），工具栏左侧加 `Adw.ViewSwitcher`。源预览从固定 200 高改为全尺寸（`Vexpand=true`）。
- 状态隐藏：`editing_existing_frames` 时整个 `source_section` 与 `attempt_section`（非空时）`Visible=false`（原 Sensitive=false 逻辑替换）；output_attempts.Count==0 时 attempt_section 不创建；AI/Grid 由 ViewStack 切换（已有）。Grid 页 4 项高级参数保持折叠 Expander。
- 陷阱：partial 类的 readonly 字段若在间接方法调用（BuildContent←构造函数）里赋值会 CS0171 —— 改为字段初始化器创建（`source_section`/`preview_stack`），`attempt_section` 非 readonly 条件创建。
- 未编译（用户 ./run.bat 验证）。

## 卡片化分组（按功能）

- 原布局：所有控件用粗水平 `Gtk.Separator` 平行堆叠，无视觉边界，标题层级模糊。
- 新方案：每个功能组用 Gtk.Box + adwaita `card` css 类（libadwaita `1.x` 内置 `.card`，圆角白底背景）包裹，左列 main panel spacing 10 形成卡片间留白。
- 三个卡片：Source extraction / Output canvas (+ Output attempt 条件嵌套) / Frames。Frame tab 预览区保持原样不分卡。
- 帧列表卡内 `ScrollView.MinContentHeight = 180` 避免帧少时列表过矮；hint 移入卡内底部带 dim 样式。
- `attempt_section` 改为非 readonly 的 card widget（条件创建），编辑模式 `Visible=false` 直接隐藏整个子卡。
- 字段初始化器 `private readonly Gtk.Box source_section = CreateCard ();` —— readonly 字段不能重新赋值，但可在方法内 Append 内容；静态 helper `CreateCard()` 在字段初始化器中合法。
- 未编译（用户 ./run.bat 验证）。

## 工具栏移至底部

- 原：顶部 `BuildPreviewToolbar` 横向 box 含 ViewSwitcher/上下帧/undo/redo/guide 按钮，加 Toolbar css 类，挂在预览区上方。
- 新：顶部工具栏删除，工具按钮合并到 `BuildStatusBar` —— 整条横向 box 位于 content area 底部（分隔线下方、action area 上方），加 Toolbar css 类。
- `BuildStatusBar` 内布局：`[Source|Frame] | ◀ ▶ | ↶ ↷ | H-guide V-guide | spacer | validation_label`（label Xalign=1 右对齐，Halign=End 顶到右边）。
- 预览区获 ~34px 额外垂直空间；`BuildPreviewPanel` 直接 `Append(preview_stack)`，无间距。
- 删除整个 `BuildPreviewToolbar` 方法。

## 按钮样式现代化

- `CreateNavigationButton`（prev/next/undo/redo 共用 helper）：图标按钮加 `AdwaitaStyles.Flat` + `"circular"` —— 去掉方框背景，做成圆形。
- `fullscreen_button`：加 `AdwaitaStyles.Flat` —— 标题栏图标按钮无背景，融入 headerbar。
- `add_horizontal_guide` / `add_vertical_guide`：底部 guide 文本按钮加 `AdwaitaStyles.Flat` + `"pill"` —— 胶囊形状、hover 才显示背景。
- `submit`（Create/Save）保留 `SuggestedAction`（强调主操作）；Cancel 按钮（action area 自动添加）保持默认。
- 样式类来源：`AdwaitaStyles` 常量（已有 Flat/SuggestedAction 等）；`"circular"`/`"pill"` 是 libadwaita 内置 css 类名（未在 AdwaitaStyles 中加常量，避免改跨项目常量文件；后续如复用频繁再加）。

## 布局全面重构（工具栏归位 + Frames 分栏 + guide 图标化）

- 触发原因：用户截图反馈界面布局混乱——左列过挤、底部工具栏文本按钮丑、工具栏离预览太远、Frames 区块内部无水平分区。
- **核心改动**：
  1. **工具栏从底部移回预览区顶部**：新建 `BuildPreviewToolbar()` 挂在 `BuildPreviewPanel()` 的 preview_stack 上方。底部 `BuildStatusBar()` 精简为仅 validation_label（dim 样式、左对齐、wrap），去掉 Toolbar css 类和所有按钮。
  2. **Frames 卡片内部分左右两栏**：左侧 `Gtk.ScrolledWindow` 包 frame_list（Hexpand=true, MinContentHeight=160, Never/Automatic policy）；右侧固定 140px 宽的 pos_panel 含 Position 标题 + X/Y grid + move_root checkbox + 短 hint（MaxWidthChars=18）。不再垂直堆叠。
  3. **guide 按钮图标化**：`add_horizontal_guide`/`add_vertical_guide` 从 `NewWithLabel`+Flat+pill 文本按钮改为 `CreateNavigationButton("line-horizontal/vertical-symbolic")` —— 与 prev/next/undo/redo 统一为 Flat+circular 图标按钮。
  4. **标尺瘦身**：horizontal_ruler 24→22px，vertical_ruler 32→28px，多出 6px 给预览内容。
  5. **左列宽度** 320→300px（Frames 内部已有水平空间利用，不需要那么宽）。
  6. **间距优化**：main_row MarginBottom 去掉（让 status bar 直接贴底）；preview_panel spacing=6（toolbar 与预览紧凑）；status_bar margin=8。
- 新结构示意：
  ```
  ┌─ Source extraction card ─┐  ┌─ [Source|Frame] |◀▶|↶↷| ═ ║ |──────┐
  │ [AI|Grid] tabs           │  │                                     │
  │ C/R/W/H spinners         │  │     PREVIEW (full remaining)       │
  │ ▸ Offsets and gaps       │  │                                     │
  │ ☑ Align character        │  │                                     │
  ├─ Output canvas card ─────┤  │                                     │
  │ W/H spinners             │  │                                     │
  ├─ Frames card ────────────┤  │                                     │
  │ ┌─frame list─┐┌─position┐│  │                                     │
  │ │ ☐1:down   ││X:__ Y:_ ││  │                                     │
  │ │ ☐2:d-r    ││☑Mv root ││  │                                     │
  │ │ ...scroll ││hint...   ││  └─────────────────────────────────────┘
  │ └───────────┘└──────────┘│  8 sprites will be created.
  └───────────────────────────┘  [取消(C)]  [Create]
```

## Bug 修复：标尺点击误触 AI 分析弹窗

- 现象：点击水平标尺（horizontal_ruler）弹出 "Analyzing sprite bounds..." 模态对话框。
- 根因：`ConnectRulerAndGuidePointerEvents()` 中给 `horizontal_ruler` 绑定了 `GestureClick` → `HandleRulerClick()` → `ShowAnalysisModal()`。这是测试/原型遗留代码，标尺不应有点击行为。
- 修复：删除 `ruler_click` GestureClick 绑定、`HandleRulerClick()` 方法、以及整个 `ShowAnalysisModal()` 方法（死代码）。标尺恢复为纯展示控件。
- 未编译（用户 ./run.bat 验证）。

## 标尺显示美化

- **背景填充**：`DrawRuler` 开头用 `SetSourceRgb(0.95,0.94,0.93)` 填充整个标尺区域（adwaita 浅灰），不再透明/白色与预览区混在一起。
- **边框线**：水平标尺底部、垂直标尺右侧各画一条 `0.85/0.84/0.83` 细线，区分标尺与预览区边界。
- **三级刻度系统**：`DrawRulerTick` 参数从 `bool major` 改为 `int tier`：
  - tier=2 (major, 每5格)：全长 + 线宽1.0
  - tier=1 (medium, 第3格)：2/3 长度 + 线宽0.6
  - tier=0 (minor, 其余)：1/3 长度 + 线宽0.6
- **数字标签样式**：`DrawRulerLabel` 新增 `Color labelColor` 参数；Pango 字体设 `"8"` 号 dim 色（fg*0.55）；水平标尺 label 左移+3/+2、垂直右移-3/+3 微调对齐。
- **指针指示器**：从亮蓝 `Color(0.1,0.6,1.0,0.95)` 粗刻度改为半透明细线 `Rgba(0.15,0.45,0.8,0.6)`，线宽按标尺尺寸比例（水平=height*0.15, 垂直=width*0.12），视觉上更克制。
- **尺寸微调**：horizontal_ruler 22px（-2）、vertical_ruler 28px（-4），更紧凑。
- **CSS class**：两个 ruler 控件加 `.ruler` class，grid 加 `.ruler-preview-grid` class，便于后续全局 CSS 定制。
- 未编译（用户 ./run.bat 验证）。

## 预览区显示当前 Sprite 名字

- **需求**：帧预览（Frame tab）上方红框区域显示当前选中 sprite 的名字（如 "1: down / Frame 1"），切换帧时自动更新。
- **实现**：
  - 主文件新增 `sprite_name_label`（`Gtk.Label` + `Title4` css 类，居中显示）。
  - 新增 `UpdateSpriteNameLabel()` 方法：通过 `GetDisplayIndex(selected_frame)` + `GetFrameLabel()` 获取显示名并设置到 label。
  - `SelectFrame()` 末尾调用 `UpdateSpriteNameLabel()` —— 每次选中帧（点击列表/上下按钮）都刷新。
  - Layout.cs `BuildRulerPreview()` 改为返回 wrapper：上方 `name_bar`（居中放 label）+ 下方 ruler+preview grid；grid 的 Attach 行号下移一行（标尺从 row 0→1, 预览从 row 1→2）。
- 文件：`SpritesheetSplitDialog.cs`（字段+构造+方法）、`Layout.cs`（BuildRulerPreview 布局）。

## Output canvas + Position 移至预览旁侧栏

- **需求**：将 Output canvas（画布宽高）和 Position（X/Y + Move root anchor）从左列移到预览画布右侧，形成「预览 | 设置」并排布局。
- **新结构**：
  ```
  ┌─ Left (260px) ─────┐  ┌─ Right (剩余宽度) ───────────────────────────┐
  │ Source extraction   │  │ [Source|Frame] |◀▶|↶↷|║│  │ ┌─ Sidebar(170) ─┐│
  ```
- **改动文件**：`Layout.cs`（重写 BuildContent/BuildLeftPanel/BuildRightPanel/BuildPreviewSidebar）。

## 图标按钮可见性修复

- **问题**：`Flat` + `circular` 组合让工具栏图标按钮（导航◀▶、undo↶↷、参考线║）在浅色主题下几乎不可见（变成极小的淡色圆点）。
- **修复**：
  - `CreateNavigationButton()` 去掉 `circular` 和 `Flat` → 导航/undo/redo 按钮恢复正常的图标按钮外观（有背景边框）。
  - 新增 `CreateFlatButton()` → 仅 guide 按钮使用 Flat（guide 是辅助功能，可以更低调）。
  - 工具栏按钮用 `Linked` css class 分为三组（导航组、undo 组、guide 组），视觉上更整体。
  - 工具栏间距从 4→6，加 `SetAllMargins(4)` 让呼吸感更好。
- **文件**：`Layout.cs`（CreateNavigationButton/CreateFlatButton/BuildPreviewToolbar）、主文件（guide 按钮改用 CreateFlatButton）。
- 未编译（用户 ./run.bat 验证）。

## 整体界面视觉优化

- **问题**：界面整体"丑"——控件密度不均、标签未翻译、AI 按钮硬编码中文、Source tab 切换按钮小、底部状态信息孤立。
- **改动**：
  1. **侧栏控件横向并排**：新增 `CreateRow()` helper 把 `(label, spinner)` 对横向并排（一行 `[Width:][spin][Height:][spin]`），替代 `CreateGrid()` 垂直排列。侧栏宽度 170→180。
  2. **修复硬编码中文**：AI 按钮 `\u81ea\u52a8\u5206\u6790`（自动分析）→ `Translations.GetString ("Auto analyze")`；tooltip `\u4f7f\u7528 AI \u5206\u6790\u7cbe\u70eb\u8fb9\u754c\u548c\u811a\u5e95\u951a\u70b9` → `Translations.GetString ("Use AI to detect sprite bounds and foot anchors")`；`X:` `Y:` → `Translations.GetString ("X:")` 等。
  3. **Source tab 改用 Linked 按钮组**：`Adw.ViewSwitcher` 替换为两个 `Gtk.ToggleButton` + `Linked` 类，自己处理 OnToggled 和 OnNotify 同步 `VisibleChildName`。按钮文字 "AI analysis" / "Grid" 始终可读，Homogeneous=true 等宽。
  4. **SpinButton 加 `Compact` 类**：所有 spinner 都更紧凑，控件密度下降约 30%。
  5. **统一卡片 padding**：所有 `CreateCard()` 调用处都加 `SetAllMargins(12)`，Source/Frames/Output/Position 四张卡片内边距一致。
  6. **底部状态条居中**：`validation_label` 从右对齐改为居中（`Xalign=0.5f, Halign=Center, Hexpand=true`），加 `caption` 类小字号，与 Cancel/Create 按钮同行更突出。
  7. **hint 文字小字号化**：所有提示文字加 `caption` 类，与正文区分。
- **陷阱**：`AdwaitaStyles.Caption` 常量不存在，需用字符串字面量 `"caption"`（adwaita 内置 css 类名）。
- **文件**：`SpritesheetSplitDialog.Layout.cs`（重写 BuildPreviewSidebar / BuildSourceSection / BuildFrameListSection / BuildSourceModeTabs / BuildSmartAnalyzeControls / helpers）。
- 未编译（用户 ./run.bat 验证）。
  │ [AI|Grid] tabs      │  ├──────────────────────────┤ │ Output canvas   ││
  │ C/R/W/H + offsets   │  │                          │ │ Width/Height    ││
  │ ☑ Align char        │  │   PREVIEW (全宽扩展)     │ │                 ││
  ├─ Frames ────────────┤  │                          │ ├─ Position ──────┤│
  │ ☐1: down / Frame 1  │  │                          │ │ X:__ Y:__      ││
  │ ☐2: d-r / Frame 1   │  │                          │ │ ☑Move root      ││
  │ ...scroll...         │  │                          │ │ hint            ││
  └──────────────────────┘  └──────────────────────────┘ └────────────────┘│
  ```
- **Layout.cs 重构**：
  - 删除 `BuildSettingsPanel()`、`BuildOutputSection()`、`BuildFramesSection()`。
  - 新增 `BuildLeftPanel()` = Source section + Frame list section（左列，260px）。
  - 新增 `BuildRightPanel()` = 水平行：`BuildPreviewPanel()` | `BuildPreviewSidebar()`。
  - 新增 `BuildPreviewSidebar()` = 固定 170px 宽的垂直卡片列：Output canvas card + Position card。
  - 新增 `BuildFrameListSection()` = 简化的 Frames 卡片（只有滚动列表，无位置面板）。
- **Output canvas 标签缩短**："Canvas width:" → "Width:"，"Canvas height:" → "Height:"（侧栏空间有限）。
- 左列从 320px 缩到 260px（不再容纳 Output/Position），帧列表 MinContentHeight 从 160→200（独占更多垂直空间）。
